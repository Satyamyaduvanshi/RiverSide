import { CreateStudioForm } from '../../components/studio/CreateStudioForm';

export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <CreateStudioForm />
    </main>
  );
}